# It is a best practice to pin dependencies to ensure reproducible builds.
# These versions are examples. You can generate your current versions with `pip freeze > requirements.txt`.
fastapi==0.110.0
uvicorn==0.29.0
gunicorn==21.2.0
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.29
alembic==1.13.1
psycopg2-binary==2.9.9 # Sync driver for Alembic
asyncpg==0.29.0 # Async driver for PostgreSQL

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==2024.1